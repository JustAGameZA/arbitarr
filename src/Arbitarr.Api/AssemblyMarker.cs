namespace Arbitarr.Api;

/// <summary>
/// Placeholder marker for the Arbitarr.Api assembly.
/// Populated in Step 4 — Torznab/Newznab rendering: capability documents, search endpoints,
/// golden-XML serialization, and the download proxy. Left as a plain class library at Step 1;
/// whether it becomes an ASP.NET Core minimal-API host or stays a class library consumed by
/// Arbitarr.Host is decided when Step 4 defines the actual endpoints. Only its dependency
/// on Arbitarr.Core is established here.
/// </summary>
internal static class AssemblyMarker
{
}
